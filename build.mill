//| mill-version: 1.1.2
//| mill-jvm-version: system

import mill._
import mill.scalalib._

object ysyxsoc extends SbtModule {
  override def moduleDir = super.moduleDir / os.up

  override def scalaVersion = "2.13.14"

  override def scalacOptions = Seq(
    "-language:reflectiveCalls",
    "-Ymacro-annotations",
    "-Ytasty-reader",
  )

  override def mvnDeps = Seq(
    mvn"org.chipsalliance::chisel:7.0.0-M2",
    mvn"com.lihaoyi::mainargs:0.5.4",
    mvn"org.json4s::json4s-jackson:4.0.6",
    mvn"org.scala-lang:scala-reflect:2.13.14",
    mvn"com.lihaoyi::sourcecode:0.3.1",
  )

  override def scalacPluginMvnDeps = Seq(
    mvn"org.chipsalliance:::chisel-plugin:7.0.0-M2",
  )

  override def sources = Task.Sources(
    moduleDir / "src",
    moduleDir / "rocket-chip" / "src" / "main" / "scala",
    moduleDir / "rocket-chip" / "macros" / "src" / "main" / "scala",
    moduleDir / "rocket-chip" / "dependencies" / "hardfloat" / "hardfloat" / "src" / "main" / "scala",
    moduleDir / "rocket-chip" / "dependencies" / "cde" / "cde" / "src",
    moduleDir / "rocket-chip" / "dependencies" / "diplomacy" / "diplomacy" / "src",
  )
}
